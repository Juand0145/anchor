pymupdf>=1.27
anthropic>=0.100
python-dotenv
openai>=1.40
# Notebook (pandas DataFrame, Jupyter): pip install -e ".[dev]"
# openpyxl is optional; the core library writes JSON only (no Excel dependency).
